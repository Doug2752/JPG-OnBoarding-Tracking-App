import React from 'react';
import ReactDOM from 'react-dom/client';
import OBApp from './app/OBApp.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(React.createElement(OBApp));
